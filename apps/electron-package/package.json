{
  "name": "comflowy",
  "version": "0.0.0",
  "private": true,
  "main": "layers/main/dist/index.js",
  "scripts": {
    "start": "electron ./",
    "compile": "cd ../electron-backend && npm run compile && cd ../electron-package && npm run package",
    "package": "electron-forge package"
  },
  "devDependencies": {
    "@electron-forge/cli": "^7.2.0",
    "cli-truncate": "^4.0.0",
    "cross-env": "7.0.3",
    "dts-for-context-bridge": "0.7.1",
    "electron": "^28.0.0",
    "electron-devtools-installer": "3.2.0"
  }
}
